# Mouse

Since running a Google Colab notebook will disconnect after a certain period of inactivity, this script simulates mouse movement to keep the session active.

## How to run

1. Create a new uv environment and install the required dependencies:

```bash
uv create mouse-env
uv activate mouse-env
uv sync
```

2. Run the script:

```bash
python main.py
```